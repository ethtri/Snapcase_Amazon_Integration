{
  "name": "snapcase-amazon-printful-integration",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "lint": "node scripts/noop.js lint",
    "test": "node scripts/noop.js test",
    "sync:status": "node scripts/noop.js sync-status"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
